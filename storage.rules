rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only authenticated users can read or write documents
      allow read, write: if request.auth != null 
                         && request.resource.size < 50 * 1024 * 1024; // 50MB Limit
    }
  }
}

